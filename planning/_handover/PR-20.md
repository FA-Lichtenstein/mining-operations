# PR-20 — Shortlist briefs (user gate)

## Inputs read

- `../portfolio-ideas-catalog.html` — PR-19 ranked catalog, composite scores, module IDs, Firestore schema sketch, pillar shortlist table
- `PR-19.md` — gate results, recommended pillars, MDP duplication note, ID legend (idea-XX vs rank #)
- `theme-taxonomy.md` — T1–T6 mix guidance (T1+T2+T3 + optional T4/T6)
- `../cv-ukwazi-alignment.html` — CV proof gaps (T1=2, T3=3, T5=5, T6=1); persona and bullet mapping
- `../angular-firebase-or-stack.html` — Learn/Decide routes, Worker boundaries, Firestore patterns (PR-04)
- `../synthetic-data-generator-spec.html` — `gen-block-model`, `gen-truck-cycle`, `gen-plan-survey-var` outputs and JSON configs
- `../sa-mining-context.html` — `syn-pgm-bushveld-01` PGM open-pit persona
- `../index.html` — registry row `id="pr-20"` updated to complete

## Key findings

- **Deliverable:** `portfolio-shortlist-briefs.html` — four build-ready briefs with problem/persona, MVP vs stretch, Firestore schema + JSON samples, Angular module breakdown, mermaid Firebase architecture, 5-minute demo script, week-by-week build order, and CV bullet per project.
- **Shortlist confirmed (default pillar mix):** `idea-02` T2 DES (4.70), `idea-03` T3 reconciliation (3.93), `idea-01` T1 schedule (3.86), `idea-06` T5 fleet MDP (3.93).
- **Fourth-slot decision:** **idea-06** over **idea-10** closure MC — same composite tier as idea-03 (3.93 vs 3.55), CV proof 5 vs 1, T4 breadth already on idea-02 IPCC sidebar; T6 remains optional deferred breadth.
- **Shared persona:** Naledi Mokoena, Ukwazi Quantitative Planning Analyst at `syn-pgm-bushveld-01` (PGM open pit, Limpopo).
- **Recommended build sequence:** T2 (weeks 1–3) → T3 (4–6) → T5 (7–9) → T1 (10–16); DES flagship first; schedule optimizer last (Effort L).
- **Module IDs locked:** `haulage-des`, `reconciliation-dashboard`, `fleet-capacity-mdp`, `pit-schedule-gantt`.
- **Cross-module links documented:** fleet MDP downtime → haulage DES; schedule plan tonnes → reconciliation dashboard.
- **Artifact scorecard:** Ukwazi 5, CV 4, Stack 5, Data 5, Diff 5, Effort M → composite **4.47**.

## Open questions / risks

- **T1 scale honesty:** idea-01 must never imply Whittle parity — precomputed toy MIP + Cloud Function stretch only.
- **T6 gap persists:** closure MC deferred; if Ukwazi interview emphasises sustainability, swap idea-06 for idea-10 at cost of CV proof.
- **Synthetic data disclaimer:** mandatory on all four demos per PR-07b.
- **Total timeline:** ~16 weeks solo sequential; parallel T2+T3 after shared shell reduces to ~10 weeks.
- **PR-07 ETL still optional:** `benchmark_kpi.csv` and `entities.csv` referenced but not yet materialised on disk.

## Review fixes (2026-05-27)

- **Mermaid non-render (critical):** Architecture diagrams used `<pre class="mermaid"><code>…</code></pre>` with no mermaid.js — diagrams showed as raw text. Fixed markup (direct `pre.mermaid` content) and added CDN init script.
- **Route / module ID drift (polish):** Reconciliation and pit-schedule mermaid paths used short slugs (`/decide/reconciliation`, `/decide/pit-schedule`) while module IDs are `reconciliation-dashboard` and `pit-schedule-gantt`; fleet routes used `fleet-mdp` vs module `fleet-capacity-mdp`. Aligned Decide/Learn paths to module IDs; added §4 route mapping table.
- **portfolioSeeds consistency (polish):** Only idea-02 schema cited `portfolioSeeds/{ideaId}`; added mirror comment on idea-01, idea-03, idea-06 blocks per PR-19 catalog schema.
- **Catalog back-link (polish):** `portfolio-ideas-catalog.html` footer and T6 row now link to completed PR-20 briefs doc.

## Verification (review pass)

| Check | Result |
|-------|--------|
| 4 briefs with all required sections | ✓ Problem/persona, MVP vs stretch, Firestore+JSON, Angular modules, mermaid arch, 5-min demo, build order, CV bullet |
| Pillar mix T1+T2+T3 + optional | ✓ T1 idea-01, T2 idea-02, T3 idea-03, T5 idea-06 (fourth slot); T4 via idea-02 IPCC stretch; T6 idea-10 deferred |
| Firestore schemas vs PR-19 catalog | ✓ `haulageDesRuns`, `reconSnapshots`, `mdpPolicies`/`fleetSimRuns`, `scheduleScenarios`/`mipRuns` + `portfolioSeeds` |
| Demo scripts (~5 min, 6 beats each) | ✓ All four briefs; synthetic disclaimer on final beat |
| Generator IDs vs PR-07b | ✓ `gen-truck-cycle`, `gen-plan-survey-var`, `gen-block-model`; `fleet_state.json` from truck-cycle |
| Module IDs vs PR-07b / PR-19 | ✓ `haulage-des`, `reconciliation-dashboard`, `fleet-capacity-mdp`, `pit-schedule-gantt` |
| Registry row complete | ✓ `index.html#pr-20` status complete, output linked |

## Recommended next tasks

- **Implementation Wave 4** — Scaffold Angular monorepo shell (PR-04 Learn index) + `haulage-des` MVP as first code deliverable.
- **PR-07b CLI** — TypeScript generators emitting seed files referenced in briefs.
- **Optional T6 swap** — If user requests sustainability pillar, produce addendum brief for `idea-10` without removing T5.
- **Firestore seed script** — Populate `portfolioSeeds/{ideaId}` from catalog JSON for demo registry cards.

## Optional extensions (not applied)

- **T6 addendum brief** — One-page `idea-10` closure MC brief (module `closure-liability-mc`) if sustainability narrative needed; swap or add alongside T5 without removing fleet MDP.
- **`portfolio-seeds.json` export** — Machine-readable seed manifest mirroring Firestore `portfolioSeeds` for gate scripts (from PR-19 optional list).
- **Learn routes for T1/T3** — Optional `/learn/pit-schedule-gantt` (Gorai precedence primer) and `/learn/reconciliation-dashboard` (Sloan flexible-budget explainer); currently Decide-first with in-app sidebars.
- **PR-07b CLI** — TypeScript generators emitting seed files referenced in briefs (`benchmark_kpi.csv` still pending PR-07 ETL).
- **Offline mermaid fallback** — Static SVG exports if CDN unavailable in air-gapped review.
