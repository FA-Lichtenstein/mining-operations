# PR-20 — Shortlist briefs (user gate)

## Inputs read

- `../portfolio-ideas-catalog.html` — PR-19 ranked catalog, composite scores, module IDs, Firestore schema sketch, pillar shortlist table
- `PR-19.md` — gate results, recommended pillars, MDP duplication note, ID legend (idea-XX vs rank #)
- `theme-taxonomy.md` — T1–T6 mix guidance (T1+T2+T3 + optional T4/T6)
- `../cv-ukwazi-alignment.html` — CV proof gaps (T1=2, T3=3, T5=5, T6=1); role mapping and bullet mapping
- `../angular-firebase-or-stack.html` — Learn/Decide routes, Worker boundaries, Firestore patterns (PR-04)
- `../synthetic-data-generator-spec.html` — `gen-block-model`, `gen-truck-cycle`, `gen-plan-survey-var` outputs and JSON configs
- `../sa-mining-context.html` — `syn-pgm-bushveld-01` PGM open-pit demo site profile
- `../index.html` — registry row `id="pr-20"` updated to complete

## Key findings

- **Deliverable:** `portfolio-shortlist-briefs.html` — five build-ready briefs with problem/role, MVP vs stretch, Firestore schema + JSON samples, Angular module breakdown, mermaid Firebase architecture, 5-minute demo script, week-by-week build order, and CV bullet per project.
- **Shortlist confirmed (user gate):** `idea-02` T2 DES (4.70), `idea-03` T3 reconciliation (3.93), `idea-06` T5 fleet MDP (3.93), `idea-01` T1 schedule (3.86), `idea-10` T6 closure MC (rank #9, 3.55).
- **Fifth project added (2026-05-27):** **idea-10** closure liability scenario model — completes T6 coverage alongside T1/T2/T3/T5; T4 remains via idea-02 IPCC sidebar.
- **Shared user role:** Quantitative Planning Analyst, Ukwazi Mineral Services — embedded at synthetic demo site `syn-pgm-bushveld-01` (PGM open pit, Limpopo). No named fictional characters; stakeholders referenced by official title only (e.g. Technical Services superintendent).
- **Recommended build sequence:** T2 (weeks 1–3) → T3 (4–6) → T5 (7–9) → T1 (10–16) → T6 (17–19); DES flagship first; closure MC last.
- **Module IDs locked:** `haulage-des`, `reconciliation-dashboard`, `fleet-capacity-mdp`, `pit-schedule-gantt`, `closure-liability-mc`.
- **Cross-module links documented:** fleet MDP downtime → haulage DES; schedule plan tonnes → reconciliation dashboard.
- **Artifact scorecard:** Ukwazi 5, CV 4, Stack 5, Data 5, Diff 5, Effort M → composite **4.47**.

## Open questions / risks

- **T1 scale honesty:** idea-01 must never imply Whittle parity — precomputed toy MIP + Cloud Function stretch only.
- **T6 closure MC:** idea-10 included as Brief 5; CV proof remains weakest pillar — demo targets decision-analysis / MC skills, not SAMVAL sign-off.
- **Synthetic data disclaimer:** mandatory on all five demos per PR-07b.
- **Total timeline:** ~19 weeks solo sequential; parallel T2+T3 after shared shell reduces to ~12 weeks.
- **PR-07 ETL still optional:** `benchmark_kpi.csv` and `entities.csv` referenced but not yet materialised on disk.

## Review fixes (2026-05-27)

- **Fifth brief — idea-10 T6 (user gate):** Added Brief 5 closure liability MC (`closure-liability-mc`); summary table, shell integration, scorecard, and role framing updated for full T1–T6 coverage (T4 via idea-02 sidebar).
- **Named persona removed (2026-05-29):** Replaced fictional character with role-based framing throughout `portfolio-shortlist-briefs.html` (§2 shared user role; §3 problem/role headings).
- **Mermaid non-render (critical):** Architecture diagrams used `<pre class="mermaid"><code>…</code></pre>` with no mermaid.js — diagrams showed as raw text. Fixed markup (direct `pre.mermaid` content) and added CDN init script.
- **Route / module ID drift (polish):** Reconciliation and pit-schedule mermaid paths used short slugs (`/decide/reconciliation`, `/decide/pit-schedule`) while module IDs are `reconciliation-dashboard` and `pit-schedule-gantt`; fleet routes used `fleet-mdp` vs module `fleet-capacity-mdp`. Aligned Decide/Learn paths to module IDs; added §4 route mapping table.
- **portfolioSeeds consistency (polish):** Only idea-02 schema cited `portfolioSeeds/{ideaId}`; added mirror comment on idea-01, idea-03, idea-06 blocks per PR-19 catalog schema.
- **Catalog back-link (polish):** `portfolio-ideas-catalog.html` footer and T6 row now link to completed PR-20 briefs doc.

## Verification (review pass)

| Check | Result |
|-------|--------|
| 4 briefs with all required sections | ✓ Problem/role, MVP vs stretch, Firestore+JSON, Angular modules, mermaid arch, 5-min demo, build order, CV bullet |
| Pillar mix T1+T2+T3 + optional | ✓ T1 idea-01, T2 idea-02, T3 idea-03, T5 idea-06, T6 idea-10 (Brief 5); T4 via idea-02 IPCC stretch |
| Firestore schemas vs PR-19 catalog | ✓ `haulageDesRuns`, `reconSnapshots`, `mdpPolicies`/`fleetSimRuns`, `scheduleScenarios`/`mipRuns`, `closureScenarios`/`mcRuns` + `portfolioSeeds` |
| Demo scripts (~5 min, 6 beats each) | ✓ All five briefs; synthetic disclaimer on final beat |
| Generator IDs vs PR-07b | ✓ `gen-truck-cycle`, `gen-plan-survey-var`, `gen-block-model`; `closure_risk.csv` (PR-07 T6 package) |
| Module IDs vs PR-07b / PR-19 | ✓ `haulage-des`, `reconciliation-dashboard`, `fleet-capacity-mdp`, `pit-schedule-gantt`, `closure-liability-mc` |
| Registry row complete | ✓ `index.html#pr-20` status complete, output linked |

## Recommended next tasks

- **Implementation Wave 4** — Scaffold Angular monorepo shell (PR-04 Learn index) + `haulage-des` MVP as first code deliverable.
- **PR-07b CLI** — TypeScript generators emitting seed files referenced in briefs.
- **Optional T6 swap** — N/A; idea-10 included as Brief 5 per user gate (2026-05-27).
- **Firestore seed script** — Populate `portfolioSeeds/{ideaId}` from catalog JSON for demo registry cards.

## Optional extensions (not applied)

- **T6 addendum brief** — ~~Deferred~~ **Done:** full Brief 5 for `idea-10` / `closure-liability-mc` in `portfolio-shortlist-briefs.html`.
- **`portfolio-seeds.json` export** — Machine-readable seed manifest mirroring Firestore `portfolioSeeds` for gate scripts (from PR-19 optional list).
- **Learn routes for T1/T3** — Optional `/learn/pit-schedule-gantt` (Gorai precedence primer) and `/learn/reconciliation-dashboard` (Sloan flexible-budget explainer); currently Decide-first with in-app sidebars.
- **PR-07b CLI** — TypeScript generators emitting seed files referenced in briefs (`benchmark_kpi.csv` still pending PR-07 ETL).
- **Offline mermaid fallback** — Static SVG exports if CDN unavailable in air-gapped review.
