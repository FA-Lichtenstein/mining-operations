{
  "name": "mining-operations",
  "private": true,
  "description": "Mining operations OR portfolio — Angular app in app/",
  "scripts": {
    "start": "npm run start --prefix app",
    "build": "npm run build --prefix app",
    "test": "npm run test --prefix app",
    "test:engine": "npm run test:engine --prefix app",
    "test:tools": "npm run test:tools --prefix app"
  }
}
