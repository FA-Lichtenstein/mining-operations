Spin up sub-agents to implement one PR at a time. Work in serial, not parallel. Use composer 2.5 (important, NOT fast mode). After each PR, create a reviewer subagent to audit and evaluate the implementation quality, implement corrections to critical errors, implement minor quality polishes, and note down ideas for extending scope to add value. Scope extensions should not be implemented by reviewers, only documented. The workflow should be, subagent 1 implements PR 1, subagent2 audits, evaluates, patches and commits PR 1, subagent 3 implements PR2, subagent 4 audits, evaluates, patches and commits PR2, etc. When all PRs have been committed, spin up one final subagent to review the whole plan implementation and each of the noted scope extensions. This agent should correct critical errors, implement quality polishes, and consolidate a final list of proposed scope extensions. It should not implement any scope extensions. Then you should report back to me.